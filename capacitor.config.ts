import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cargo.proverne',
  appName: 'Proverne Cargo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  },
};

export default config;
